// when you want local mobile access or access from other device on the same network as the server,
// set localhost to the local IPV4 address instead
export const environment = {
  production: true,
  apiUrl: 'http://localhost:3000'
};
